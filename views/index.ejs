<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Index</title>
        
    </head>
    <body>
        <h1>Movies Index Page</h1>
        <% for(let i = 0; i < arrayOfMovies.length; i++) { %>
                <br>The <a href="/movies/<%=arrayOfMovies[i]._id%>"><%= arrayOfMovies[i].name%></a> movie.<br>
        <!-- Delete --->      
                <form action="/movies/<%=arrayOfMovies[i]._id%>?_method=DELETE" method="POST">
                    <button type="submit">Delete</button><br>
                </form> 
                
        <!-- Edit --->
                <a href="/movies/<%=arrayOfMovies[i]._id%>/edit"><button type="submit">Edit</button></a><br>
              
        <% } %>

        <!-- New --->
        <br><a href="/movies/new"><button type="submit">Create New</button></a>

    
    </body>
</html>